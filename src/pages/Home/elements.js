import styled from "styled-components";

export const Content = styled.object`
  height: 100vh;
  width: 100vw;
`;

export const Header = styled.header`
  position: sticky;
  height: 80px;
  top: 0;
  z-index: 10;
`;

export const Main = styled.main`
  position: relative;
  top: 0;
  z-index: 1;
`;

export const Footer = styled.footer`
  position: relative;
  top: 0;
  z-index: 1;
`;